apiVersion: spacegate.idealworld.group/v1
kind: SgFilter
metadata:
  name: filters
spec:
  filters:
    - code: status
      config:
        title: Status
  targetRefs:
    - kind: httproute
      name: echo